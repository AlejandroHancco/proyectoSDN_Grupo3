<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Panel de Invitado</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    {% include 'includes/navbar.html' %}

    <div class="container py-5">
        <h2 class="text-center mb-4">Bienvenido, {{ usuario.names }}</h2>

        <!-- Cursos Disponibles -->
        <div class="card shadow-sm mb-5">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">Cursos Disponibles</h5>
            </div>
            <div class="card-body p-0">
                {% if cursos_disponibles %}
                    <div class="table-responsive">
                        <table class="table table-striped mb-0 align-middle text-center">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>Nombre</th>
                                    <th>Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for curso in cursos_disponibles %}
                                <tr>
                                    <td>{{ curso.idcurso }}</td>
                                    <td>{{ curso.nombre }}</td>
                                    <td>{{ curso.estado }}</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                {% else %}
                    <div class="p-3">
                        <p class="text-center text-muted mb-0">No hay cursos disponibles actualmente.</p>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>

